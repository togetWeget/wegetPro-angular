import { SuperModalDirDirective } from './super-modal-dir.directive';

describe('SuperModalDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperModalDirDirective();
    expect(directive).toBeTruthy();
  });
});
