import { BlocTitleDirective } from './bloc-title.directive';

describe('BlocTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocTitleDirective();
    expect(directive).toBeTruthy();
  });
});
