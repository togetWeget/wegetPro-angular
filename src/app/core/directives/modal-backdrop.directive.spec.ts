import { ModalBackdropDirective } from './modal-backdrop.directive';

describe('ModalBackdropDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalBackdropDirective();
    expect(directive).toBeTruthy();
  });
});
