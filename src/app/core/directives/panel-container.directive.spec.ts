import { PanelContainerDirective } from './panel-container.directive';

describe('PanelContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelContainerDirective();
    expect(directive).toBeTruthy();
  });
});
