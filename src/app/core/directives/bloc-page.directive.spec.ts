import { BlocPageDirective } from './bloc-page.directive';

describe('BlocPageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocPageDirective();
    expect(directive).toBeTruthy();
  });
});
